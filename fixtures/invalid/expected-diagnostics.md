# 期待診断

- `TM003 unexpected`: 未知の設定キーを拒否する。
- YAML の重複キー・型違反は、値を補正せずそれぞれ `TM001` / `TM003` とする。
- 管理対象ソースの ID 欠落・動的タイトル・動的 each は `TM205` / `TM202` / `TM203` とする。
- owner / parent / refs は継承せず、参照切れ・種類違反・循環を別の診断にする。
